trigger:
  branches:
    include:
      - main

jobs:
- job: TerraformInitPlan
  displayName: Terraform Init and Plan
  pool: self_test
  steps:
  - task: TerraformInstaller@1
    inputs:
      terraformVersion: 'latest'

  - task: TerraformTask@5
    inputs:
      provider: 'azurerm'
      command: 'init'
      workingDirectory: '$(System.DefaultWorkingDirectory)/todoapp_infra'
      backendServiceArm: 'apptestconnect01ad'
      backendAzureRmStorageAccountName: 'testadtest01'
      backendAzureRmContainerName: 'tfstate'
      backendAzureRmKey: 'terraform.tfstate'

  - task: TerraformTask@5
    inputs:
      provider: 'azurerm'
      command: 'plan'
      environmentServiceNameAzureRM: 'apptestconnect01ad'

- job: ManualValidation
  displayName: Manual Validation
  dependsOn: TerraformInitPlan
  pool: server
  steps:
  - task: ManualValidation@1
    inputs:
      notifyUsers: 'rahulgtripathi@gmail.com'
      approvers: 'rahulgtripathi@gmail.com'
      instructions: 'check please'

- job: TerraformApply
  displayName: Terraform Apply
  dependsOn: ManualValidation
  pool: self_test
  steps:
  - task: TerraformTask@5
    inputs:
      provider: 'azurerm'
      command: 'apply'
      environmentServiceNameAzureRM: 'apptestconnect01ad'
